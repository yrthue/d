#include "World.h"

World::World(int chunk_size, int size_x_chunks,
  int size_y_chunks)
{
  for(int ix = 0; ix < size_x_chunks; ix++) {
    for(int iy = 0; iy < size_y_chunks; iy++) {
      
    }
  }
}
